{
    "name": "figma-to-unity-plugin",
    "version": "0.0.2",
    "description": "Figma plugin to export designs as Unity uGUI-ready ZIP (manifest.json + PNGs)",
    "scripts": {
        "build": "rimraf dist && node esbuild.config.mjs",
        "watch": "node esbuild.config.mjs --watch",
        "server": "cd server && bun run src/index.ts",
        "dev": "rimraf dist && node esbuild.config.mjs && cd server && bun run src/index.ts",
        "clean": "rimraf dist"
    },
    "devDependencies": {
        "@figma/plugin-typings": "^1.106.0",
        "esbuild": "^0.24.0",
        "rimraf": "^6.0.0",
        "typescript": "^5.6.0"
    }
}